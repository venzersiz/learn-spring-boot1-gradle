plugins {
    id "java"
    id "org.springframework.boot" version "1.5.22.RELEASE"
}

//group = "learn"
//version = "0.1.0"

jar {
    baseName = "learn-spring-boot1-gradle"
    version = "0.1.0"
}

repositories {
    mavenCentral()
}

dependencies {
    compile "org.springframework.boot:spring-boot-starter-web"
}
